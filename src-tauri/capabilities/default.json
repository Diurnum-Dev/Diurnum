{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Diurnum desktop app.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-badge-count",
    "dialog:allow-open",
    "opener:allow-open-path",
    "window-state:default"
  ]
}
